{
    "version": 2,
    "builds": [
        {
            "src": "uvicorn main:app --reload",
            "use": "@vercel/python"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "uvicorn main:app --reload"
        }
    ]
}